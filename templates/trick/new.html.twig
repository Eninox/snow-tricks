{% extends 'base.html.twig' %}

{% block title %}New Trick{% endblock %}

{% block body %}

    <div class="section section-post-header">

        <div class="container">
            <div class="title">
                <h1>Créer un trick</h1>
            </div>

            {{ include('trick/_form.html.twig') }}

            <div class="btn-group">
                <a class="btn btn-secondary" href="{{ path('app_trick_index') }}">Revenir aux tricks</a>
            </div>
        </div>

    </div>
{% endblock %}
